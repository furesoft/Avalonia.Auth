<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:Avalonia.Auth.Controls">

    <Style Selector="controls|AuthContext">
        <Setter Property="Background" Value="#e2e2e2" />
        <Setter Property="Template">
            <ControlTemplate>
                <Border x:Name="PART_Border"
                        CornerRadius="10"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="1.5"
                        Background="{TemplateBinding Background}"
                        Padding="14,8">
                    <StackPanel Spacing="5">
                        <Label Content="{Binding Options.Title, RelativeSource={RelativeSource TemplatedParent}}"
                               FontSize="20"
                               FontWeight="Bold"
                               Foreground="#222"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>

                        <TextBox x:Name="PART_EmailBox"
                                 Watermark="Username"
                                 Margin="0,0,0,5"
                                 Width="220"
                                 HorizontalAlignment="Center"/>
                        <TextBox x:Name="PART_PasswordBox"
                                 Watermark="Password"
                                 Margin="0,0,0,10"
                                 Width="220"
                                 HorizontalAlignment="Center"
                                 PasswordChar="*"/>
                        <controls:Link Title="Password forgotten?" />
                        <controls:Link Title="Register" />

                        <Button x:Name="PART_LoginButton"
                                Content="Login"
                                Width="220"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,10"/>

                        <ItemsControl ItemsSource="{TemplateBinding Providers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <controls:AuthProviderButton
                                        Provider="{Binding}"
                                        Title="{Binding Name}"
                                        Background="{Binding Background}"
                                        Foreground="{Binding Foreground}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Spacing="5" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </ControlTemplate>
        </Setter>
    </Style>

</Styles>